{
  "framework": "nextjs",
  "buildCommand": "node scripts/switch-provider.js postgresql && npx prisma generate && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "devCommand": "npm run dev",
  "env": {
    "NEXT_PUBLIC_APP_URL": "https://lms-platform.vercel.app"
  },
  "github": {
    "silent": true
  }
}
