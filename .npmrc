# The package-lock.json was generated with npm's legacy peer resolution
# (react@19 + @uploadthing/react@6.6.0, which only declares peer support up
# to react@18). `npm ci` / `npm install` fail with ERESOLVE without this.
legacy-peer-deps=true
